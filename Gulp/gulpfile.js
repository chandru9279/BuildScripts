require('./Framework/setup.js').setup();

/* TODO: Make the buildscripts folder a separate git repo, so it can be submoduled into other projects
 * By convention it will look for BuildConfigs next to BuildScripts repo location.
 */